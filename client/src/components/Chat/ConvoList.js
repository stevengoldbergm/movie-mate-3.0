import React from "react";

const ConvoList = () => {
  // Query list of friends
  // generate List of potential conversations based on context

  // Open chat window
    // If conversation exists, load conversation
    // If conversation does not exist, create new conversation

return (
    <article className="message is-info">
      doot
    </article>
  )
}
export default ConvoList;